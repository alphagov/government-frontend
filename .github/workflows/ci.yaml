name: CI

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    name: Test
    uses: alphagov/govuk-infrastructure/.github/workflows/test-rails.yaml@main
    with:
      requiresJavaScript: true
