// Will be removed after AB finishes

$(window).load(function () {
  // If there is an error summary, set focus to the summary
  if ($('.js-error-summary').length) {
    $('.js-error-summary').focus()
  }
})
