<% if @content_item.available_translations.any? && @content_item.available_translations.length > 1 %>
  <%= render 'components/translation-nav',
      translations: @content_item.available_translations %>
<% end %>
